MEMORY 
{
    RAM: 
        start = $03FE, 
        size = $0402, 
        type = rw, 
        file = %O;
}
SEGMENTS 
{
    LOADADDR: 
        load = RAM, 
        type = ro;
    CODE: 
        load = RAM, 
        type = ro;
    IO: 
        load = RAM, 
        type = ro;
}
